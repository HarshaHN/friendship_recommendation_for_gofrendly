https://harshahn@bitbucket.org/harshahn/ml.git

cd harsha/code-server
PASSWORD=harsha ./01_code-server/code-server --host 0.0.0.0 --port 10000

%First time
git init
git add --all
git commit -m "Initial Commit"
git config --global user.email connectharsha13@gmail.com
git config --global user.name harshahn
git remote add origin https://harshahn@bitbucket.org/harshahn/workspace.git
git push -u origin master
git remote rm origin

%Often
git push -u origin master
git status
git add .
git commit -m "name"

#key gen
cd
cd .ssh
ls
ssh-keygen -t rsa -b 4096
cat id_rsa.pub

#MySQL 
Windows:
https://www.youtube.com/watch?v=C9AGrSJ6ZB0&t=183s
After mysql server, import gofrendly database and must be displayable on the schema.
Choose MySQL 8+
To reuse: https://stackoverflow.com/questions/32022580/failed-to-connect-to-mysql-at-localhost3306-with-user-root

Linux:
D:\GoFrendly\archive\gofrendly.sql
sudo su
mysql
CREATE USER 'gofrendly'@'localhost' IDENTIFIED BY 'gofrendly';
GRANT SELECT ON * . * TO 'gofrendly'@'localhost';
FLUSH PRIVILEGES;
SHOW GRANTS gofrendly;
quit

mysql -u gofrendly -p gofrendly < data/gofrendly.sql
exit

%BERT
bert-serving-start -model_dir /data/cased_L-12_H-768_A-12/ -num_worker=1
